# Security: Block packages published less than 3 days ago
minimum-release-age=4320

# Security: Don't auto-run install scripts (prevents malicious postinstall)
ignore-scripts=true

# Security: Fail if lockfile is out of sync
frozen-lockfile=true

# Use exact versions by default (no ^ or ~)
save-exact=true
